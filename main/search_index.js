var documenterSearchIndex = {"docs":
[{"category":"section","location":"quickstart/#1D-Burgers-equation","page":"Quick Start","text":"The viscous Burgers equation u_t + (u^22)_x = nu u_xx is nonlinear, so the adjoint of each time step depends on the state at that step. A reverse pass therefore needs every forward state, and storing them all takes memory proportional to the number of time steps. Checkpointing stores a few of them and recomputes the rest.\n\nWe start from a Gaussian bump on the periodic unit interval. Its crest travels fastest, so the front steepens into a shock. The time loop is wrapped in @ad_checkpoint, and Revolve(10) keeps 10 of the 500 states, recomputing the others during the reverse pass.\n\n# Viscous Burgers equation u_t + (u²/2)_x = ν u_xx on the periodic unit interval.\n# The crest of a Gaussian bump travels fastest, so the wave steepens into a shock.\nusing Checkpointing\nusing Enzyme\n\nmutable struct Burgers\n    u::Vector{Float64}      # state at the current time step\n    ulast::Vector{Float64}  # state at the previous time step\n    ν::Float64              # viscosity\n    Δx::Float64\n    Δt::Float64\nend\n\nBurgers(u0; ν = 0.005, Δt = 1e-3) = Burgers(copy(u0), similar(u0), ν, 1 / length(u0), Δt)\n\n# One explicit Euler step, central differences, periodic boundaries.\nfunction step!(b::Burgers)\n    b.ulast .= b.u\n    u, n = b.ulast, length(b.u)\n    for i = 1:n\n        l = i == 1 ? n : i - 1\n        r = i == n ? 1 : i + 1\n        flux = (u[r]^2 - u[l]^2) / (4 * b.Δx)\n        diffusion = b.ν * (u[r] - 2 * u[i] + u[l]) / b.Δx^2\n        b.u[i] = u[i] + b.Δt * (diffusion - flux)\n    end\n    return nothing\nend\n\n# u(x_k, T) after `steps` time steps.\nfunction probe(b::Burgers, scheme::Scheme, steps::Int, k::Int)\n    @ad_checkpoint scheme for i = 1:steps\n        step!(b)\n    end\n    return b.u[k]\nend\n\n# The gradient of u(x_k, T) with respect to the initial condition u(x, 0).\nfunction sensitivity(u0::Vector{Float64}, scheme::Scheme, steps::Int, k::Int)\n    b = Burgers(u0)\n    db = Enzyme.make_zero(b)\n    autodiff(Reverse, probe, Duplicated(b, db), Const(scheme), Const(steps), Const(k))\n    return db.u\nend\n\nsensitivity returns the gradient of u(x^* T) with respect to the whole initial condition, from one reverse pass. Here we probe the shock at x^* = 06 after T = 05:\n\nn = 256\nx = ((1:n) .- 0.5) ./ n\nu0 = exp.(-((x .- 0.3) ./ 0.1) .^ 2)\nk = argmin(abs.(x .- 0.6))                  # probe the shock at x* = 0.6\ndu0 = sensitivity(u0, Revolve(10), 500, k)  # T = 500 * Δt = 0.5\n\n<div class=\"docs-light-only\">\n\n(Image: Top: a Gaussian bump steepening into a shock between t = 0 and t = 0.5, with probe points at x = 0.45 and x = 0.60 on the final profile. Bottom: the sensitivity of each probe to the initial condition; the x = 0.45 probe depends on a narrow patch near x = 0.21, the x = 0.60 probe almost uniformly on x = 0.3 to 0.6.)\n\n</div>\n<div class=\"docs-dark-only\">\n\n(Image: Top: a Gaussian bump steepening into a shock between t = 0 and t = 0.5, with probe points at x = 0.45 and x = 0.60 on the final profile. Bottom: the sensitivity of each probe to the initial condition; the x = 0.45 probe depends on a narrow patch near x = 0.21, the x = 0.60 probe almost uniformly on x = 0.3 to 0.6.)\n\n</div>\n\nA point on the smooth ramp behind the crest (x^* = 045) depends only on a narrow patch of the initial condition near x = 021, where its characteristic starts. A point in the shock (x^* = 060) depends almost equally on everything between x = 03 and x = 06: all of that fluid has run into the shock, and the shock's position depends on how much of it there was. Its total sensitivity is almost 30 times larger.\n\nThe code is in examples/burgers.jl, and examples/burgers_plots.jl draws the figure.","title":"1D Burgers equation"},{"category":"section","location":"schemes/#Checkpointing-Schemes","page":"Schemes","text":"A checkpointing scheme may support either for or while loops or both. Each scheme is a struct derived from the abstract type Scheme and parameterized by the type of the loop body closure.\n\nmutable struct MyScheme{FT} <: Scheme end\n\nSchemes use a two-phase instantiation. The user creates a scheme parameterized by Nothing, because the loop body does not exist yet:\n\nfunction MyScheme(checkpoints::Integer; kwargs...)\n    return MyScheme{Nothing}(...)\nend\n\nWhen differentiation reaches the loop, instantiate creates the actual scheme parameterized by the loop body closure type FT. For for loops it also receives the number of iterations. For while loops that number is only known once the loop has run, so it is not passed:\n\nfunction instantiate(::Type{FT}, scheme::MyScheme{Nothing}, steps::Int) where {FT}\n    return MyScheme{FT}(...)\nend\nfunction instantiate(::Type{FT}, scheme::MyScheme{Nothing}) where {FT}\n    return MyScheme{FT}(...)\nend\n\nA scheme then implements the loop in two halves. The forward half runs in the augmented forward pass, where it replaces the loop itself. It runs the loop on body, so that body ends in the loop's final state, stores checkpoints wherever the schedule asks for them, and returns a tape for the reverse half:\n\nfunction fwd_checkpoint_for(body::Function, alg::MyScheme, range) end\nfunction fwd_checkpoint_while(body::Function, alg::MyScheme) end\n\nThe reverse half runs in the reverse pass. It restores checkpoints, recomputes iterations, and differentiates them one at a time, accumulating the adjoint in the shadow closure dbody:\n\nfunction rev_checkpoint_for(config, tape, dbody::Function, alg::MyScheme, range) end\nfunction rev_checkpoint_while(config, tape, dbody::Function, alg::MyScheme) end\n\nThe reverse half is called with the Enzyme config config, the tape returned by the forward half, the shadow closure dbody, the scheme alg, and the loop range range (for for loops).\n\nThese four functions are unexported, so a new scheme extends Checkpointing.fwd_checkpoint_for and its siblings. A for loop body is called as body(i) with the current element of range. A while loop body is called as body() and returns the loop condition. Checkpoints are written with Checkpointing.save!(alg.storage, body, i) and read back with Checkpointing.load!(body, alg.storage, i); see Storage. A single iteration i of a for loop is differentiated as below; for a while loop, drop the Const(i) argument.\n\nEnzyme.autodiff(\n    EnzymeCore.set_runtime_activity(Reverse, config),\n    Duplicated(body, dbody),\n    Const,\n    Const(i),\n)","title":"Checkpointing Schemes"},{"category":"section","location":"storage/#Storage","page":"Storage","text":"The checkpoint storage types are derived from AbstractStorage\n\nusing Checkpointing\nstruct MyStorage{FT} <: AbstractStorage end\n\nand have to implement the following functions.\n\nA constructor taking the number of checkpoints\n\nMyStorage{FT}(checkpoints::Integer) where {FT}\n\nA similar method. A scheme is created before the loop body is known, so it starts out parameterized by Nothing and is re-created with the concrete closure type once the reverse pass begins.\n\nBase.similar(storage::MyStorage, ::Type{T}) where {T}\n\nStore and restore functions. Both are unexported, so a new backend extends Checkpointing.save! and Checkpointing.load!.\n\nCheckpointing.save!(storage::MyStorage{FT}, v::FT, i::Integer) where {FT}\nCheckpointing.load!(body::FT, storage::MyStorage{FT}, i::Integer) where {FT}\n\nload! restores checkpoint i into body and returns it, leaving the stored checkpoint intact – Revolve restores the same slot more than once.","title":"Storage"},{"category":"section","location":"storage/#HDF5Storage","page":"Storage","text":"HDF5Storage lives in a package extension, so HDF5 is not loaded – and libhdf5 is not even opened – unless you ask for it:\n\nusing Checkpointing, HDF5\nscheme = Revolve(100; storage = HDF5Storage)\n\nWithout using HDF5, constructing an HDF5Storage raises an error saying so.","title":"HDF5Storage"},{"category":"section","location":"storage/#Allocation-and-the-copy-interface","page":"Storage","text":"save! and load! run once per store and restore action, so a backend that copies with deepcopy allocates a fresh copy of the entire captured state every time. On a GPU that is one device allocation per action. Backends should instead build on the two functions below, which split the work into an allocation step that runs once per slot and a copy step that allocates nothing:\n\nArrayStorage fills its slots lazily: the first save! to an index allocates that slot with checkpoint_alloc, and every later save! to the same index copies into it with checkpoint_copy!. The default checkpoint_copy! copies arrays with copyto!, so CuArray, ROCArray, oneArray and MtlArray state is copied device-to-device without leaving the GPU and without scalar indexing.","title":"Allocation and the copy interface"},{"category":"function","location":"storage/#Checkpointing.checkpoint_alloc","page":"Storage","text":"checkpoint_alloc(x)\n\nAllocate a checkpoint slot able to hold a snapshot of x.\n\nThis runs once per checkpoint slot and never inside the reverse sweep, so it is free to allocate. The default is a deepcopy, which gives the slot the same field and aliasing structure as x; checkpoint_copy! then reuses that structure on every subsequent store.\n\nStorage backends that need control over where a slot lives – pinned host memory, a device buffer, an offload pool – specialize this.\n\n\n\n\n\n","title":"Checkpointing.checkpoint_alloc"},{"category":"function","location":"storage/#Checkpointing.checkpoint_copy!","page":"Storage","text":"checkpoint_copy!(dst, src) -> dst\n\nOverwrite dst with a snapshot of src, in place.\n\ndst must have come from checkpoint_alloc applied to a value of the same type, so that the two already agree on which objects are shared. Arrays are copied with copyto!, which resolves to a device-to-device copy for GPU arrays; that is what keeps the reverse sweep free of allocation on host and device alike.\n\nAfter the call dst must be unaffected by later mutation of src, so anything that cannot be written through in place is replaced with a fresh deepcopy.\n\n\n\n\n\n","title":"Checkpointing.checkpoint_copy!"},{"category":"section","location":"rules/#Rules","page":"Rules","text":"Currently, EnzymeRules rules are provided for checkpointing. The augmented_primal rules for checkpoint_for and checkpoint_while instantiate the scheme and run its forward half, fwd_checkpoint_for or fwd_checkpoint_while, which computes the primal and stores the checkpoints. The reverse rules pass the resulting tape to the scheme's reverse half, rev_checkpoint_for or rev_checkpoint_while; see Checkpointing Schemes. Adding support for another rule system requires making the same two calls from its forward and reverse rules.","title":"Rules"},{"category":"section","location":"lib/checkpointing/#API","page":"API","text":"","title":"API"},{"category":"section","location":"lib/checkpointing/#Decorator-macros-for-loops","page":"API","text":"","title":"Decorator macros for loops"},{"category":"section","location":"lib/checkpointing/#Supported-Schemes","page":"API","text":"","title":"Supported Schemes"},{"category":"section","location":"lib/checkpointing/#Supported-Storages","page":"API","text":"","title":"Supported Storages"},{"category":"section","location":"lib/checkpointing/#Developer-variables-for-implementing-new-schemes","page":"API","text":"","title":"Developer variables for implementing new schemes"},{"category":"macro","location":"lib/checkpointing/#Checkpointing.@ad_checkpoint","page":"API","text":"@ad_checkpoint(\n    alg,\n    loop,\n)\n\nThis macro is supposed to be only used in conjunction with EnzymeRules. It does not initialize the shadowcopy. Apply the checkpointing scheme alg on the loop loop expression.\n\n\n\n\n\n","title":"Checkpointing.@ad_checkpoint"},{"category":"type","location":"lib/checkpointing/#Checkpointing.Revolve","page":"API","text":"Revolve{MT}(\n    checkpoints::Int;\n    storage::AbstractStorage = ArrayStorage{MT}(checkpoints),\n    verbose::Int = 0,\n    write_checkpoints::Bool = false,\n    write_checkpoints_period::Int = 1,\n    write_checkpoints_filename::String = \"chkp\",\n) where {MT}\n\nCreates a new Revolve object for checkpointing.\n\ncheckpoints: is the number of checkpoints used for storage.\nstorage: is the storage backend to use (default is ArrayStorage).\nverbose::Int: Verbosity level for logging and diagnostics.\nwrite_checkpoints::Bool: Whether to enable writing checkpoints (default is false).\nwrite_checkpoints_period::Int: The period for writing checkpoints (default is 1).\nwrite_checkpoints_filename::String: The filename for writing checkpoints (default is \"chkp\").\n\nReferences\n\nGriewank, A. & Walther, A. “Algorithm 799: Revolve: An Implementation of\n\nCheckpointing for the Reverse or Adjoint Mode of Computational Differentiation.” ACM Transactions on Mathematical Software.\n\nThis documentation outlines the structure, usage, and functionality of Revolve and should help users integrate the checkpointing scheme into their Julia projects.\n\n\n\n\n\n","title":"Checkpointing.Revolve"},{"category":"type","location":"lib/checkpointing/#Checkpointing.Periodic","page":"API","text":"Periodic(\n    checkpoints::Int;\n    storage::AbstractStorage = ArrayStorage{MT}(checkpoints),\n    verbose::Int = 0,\n    write_checkpoints::Bool = false,\n) where {MT}\n\nThe periodic scheme is used to store the state of the system at regular intervals and then restore it when needed.\n\ncheckpoints: is the number of checkpoints used for storage.\nstorage: is the storage backend to use (default is ArrayStorage).\nverbose::Int: Verbosity level for logging and diagnostics.\nwrite_checkpoints::Bool: Whether to enable writing checkpoints (default is false).\n\nThe loop is split into checkpoints segments of div(steps, checkpoints) or cld(steps, checkpoints) steps each, so every step is covered even when checkpoints does not divide steps. period is the length of the longest segment.\n\n\n\n\n\n","title":"Checkpointing.Periodic"},{"category":"type","location":"lib/checkpointing/#Checkpointing.Online_r2","page":"API","text":"Online_r2{FT}(\n    checkpoints::Int,\n    storage::AbstractStorage = ArrayStorage{FT}(checkpoints);\n    verbose::Int = 0,\n) where {FT}\n\nThis implements the online checkpointing scheme for r=2 as described in the paper \"New Algorithms for Optimal Online Checkpointing\" by Philipp Stumm and Andrea Walther.\n\nThe key advantage is that the number of iterations does not have to be known a priori.\n\nWith fewer than 4 checkpoints the schedule extends to loops of any length. With c ≥ 4 checkpoints it covers at most (c + 2)(c + 1)/2 - 1 iterations (14 for c = 4, 20 for c = 5); a longer loop raises an error rather than silently differentiating only part of it. Extending that range needs an Online_r3 scheme, which is not implemented.\n\nCreates a new Online_r2 object for checkpointing.\n\ncheckpoints: is the number of checkpoints used for storage.\nstorage: is the storage backend to use (default is ArrayStorage).\n\n\n\n\n\n","title":"Checkpointing.Online_r2"},{"category":"type","location":"lib/checkpointing/#Checkpointing.ArrayStorage","page":"API","text":"ArrayStorage{FT}(checkpoints::Integer)\n\nArray (RAM) storage for checkpointing.\n\nSlots are filled lazily: the first save! to a given index allocates that slot with checkpoint_alloc, and every later save! to the same index copies into it with checkpoint_copy!. A reverse sweep therefore allocates at most checkpoints copies of the state no matter how many store and restore actions the scheme performs, which is what keeps the sweep allocation- free once it is warm – on the GPU as much as on the host.\n\n\n\n\n\n","title":"Checkpointing.ArrayStorage"},{"category":"type","location":"lib/checkpointing/#Checkpointing.HDF5Storage","page":"API","text":"HDF5Storage{MT}(checkpoints::Integer; filename = tempname())\n\nA storage type for checkpointing that uses HDF5 files to store the checkpoints.\n\nRequires the HDF5 package: do using HDF5 before creating one. The type is defined here so that storage = HDF5Storage can be passed to a scheme, but everything that touches an HDF5 file lives in the CheckpointingHDF5Ext package extension, which loads automatically once HDF5 is.\n\n\n\n\n\n","title":"Checkpointing.HDF5Storage"},{"category":"type","location":"lib/checkpointing/#Checkpointing.Scheme","page":"API","text":"Scheme\n\nAbstract type from which all checkpointing schemes are derived.\n\n\n\n\n\n","title":"Checkpointing.Scheme"},{"category":"section","location":"gpu/#GPU","page":"GPU","text":"Checkpointing.jl needs no GPU-specific code or dependency. The checkpointing schedule runs on the host; each iteration of the loop body launches whatever kernels it launches. Checkpoints of device arrays stay on the device: storage slots are allocated once with Checkpointing.checkpoint_alloc and refilled with copyto! through Checkpointing.checkpoint_copy!, so a store or restore is one device-to-device copy with no allocation and no transfer to the host. This works for any array type that implements copyto! – CuArray, ROCArray, oneArray, MtlArray.\n\nexamples/heat_gpu.jl runs the heat equation of the quick start on either the host or a device:\n\nusing CUDA\ninclude(\"examples/heat_gpu.jl\")\nT, dT = heat_device(Revolve(100), 500; arraytype = CuArray)\n\nand gives the same result as on the host.","title":"GPU"},{"category":"section","location":"gpu/#Writing-a-loop-body-Enzyme-can-differentiate-on-a-device","page":"GPU","text":"These constraints come from differentiating GPU broadcasts with Enzyme, not from Checkpointing.jl, and apply equally without @ad_checkpoint:\n\nNo floating-point scalars inside a broadcast. x .= λ .* y with a Float64 λ – a struct field, or even a literal 0.5 – fails with MethodError: no method matching MixedDuplicated(::Broadcasted...). Keep coefficients in device arrays. Integer literals are fine.\nNo views. Broadcasting over @views slices of a device array aborts Enzyme with number of arg operands != function parameters. Use whole-array broadcasts; circshift expresses stencil neighbours.\nNo scalar indexing, which Enzyme cannot differentiate on a device.\n\nexamples/heat_gpu.jl shows all three: the diffusion coefficient is a device array that is zero at both ends, which keeps the boundary cells fixed and cancels the wraparound terms circshift introduces.","title":"Writing a loop body Enzyme can differentiate on a device"},{"category":"section","location":"gpu/#Performance","page":"GPU","text":"A GPU only pays off once the state is large enough to amortise kernel launches. Revolve with 20 checkpoints over 200 steps, on an RTX 4080 against one host thread:\n\ncells CPU GPU speedup\n10³ 0.003 s 0.235 s 0.01×\n10⁵ 0.374 s 0.233 s 1.6×\n10⁶ 6.14 s 0.241 s 25×\n10⁷ 98.0 s 2.22 s 44×\n\nThe largest size varies by roughly ±15% between runs.\n\nReproduce with julia --project=test/gpu test/gpu/benchmark.jl.","title":"Performance"},{"category":"section","location":"gpu/#Running-the-GPU-tests","page":"GPU","text":"The GPU tests have their own environment, so the CPU test suite never installs CUDA:\n\njulia --project=test/gpu -e 'using Pkg; Pkg.develop(path=\".\"); Pkg.instantiate()'\njulia --project=test/gpu test/gpu/runtests.jl","title":"Running the GPU tests"},{"category":"section","location":"#Checkpointing","page":"Home","text":"Checkpointing.jl provides checkpointing schemes for adjoint computations using automatic differentiation (AD) of time-stepping loops. Currently, we support the macro @ad_checkpoint, which differentiates and checkpoints a struct used in a while or for the loop with a UnitRange.\n\nEach loop iteration is differentiated using Enzyme.jl. We rely on external differentiation rule systems to integrate with AD tools applied to the code outside of the loop.\n\nThe schemes are agnostic to the AD tool being used and can be easily interfaced with any Julia AD tool. Currently, the package supports:","title":"Checkpointing"},{"category":"section","location":"#Scheme","page":"Home","text":"Revolve/Binomial checkpointing [1]\nPeriodic checkpointing\nOnline r=2 checkpointing for a while loops with a priori unknown number of iterations [2]","title":"Scheme"},{"category":"section","location":"#Rules","page":"Home","text":"EnzymeRules.jl","title":"Rules"},{"category":"section","location":"#Storage","page":"Home","text":"ArrayStorage: Stores all checkpoints values in an array of type Array\nHDF5Storage: Stores all checkpoints values in an HDF5 file (requires using HDF5; provided by a package extension)","title":"Storage"},{"category":"section","location":"#Limitations","page":"Home","text":"Currently, the package only supports UnitRange ranges in for loops. We will add range types on a per-need basis. Please, open an issue if you need support for a specific range type.\nWe only support Enzyme as the differentiation tool of the loop body. This is due to our strict requirement for a mutation-enabled AD tool in our projects. However, there is no fundamental reason why we could not support other AD tools. Please, open an issue if you need support for a specific AD tool.\nWe don't support any activity analysis. This implies that loop iterators have to be part of the checkpointed struct if they are used in the loop body. Currently, we store the entire struct at each checkpoint. This is not necessary, and we will add support for storing only the required fields in the future.","title":"Limitations"},{"category":"section","location":"#Quick-Start","page":"Home","text":"Pages = [\n    \"quickstart.md\",\n]\nDepth=1","title":"Quick Start"},{"category":"section","location":"#API","page":"Home","text":"Pages = [\n    \"lib/checkpointing.md\",\n]\nDepth = 1","title":"API"},{"category":"section","location":"#References","page":"Home","text":"[1] Andreas Griewank and Andrea Walther. 2000. Algorithm 799: revolve: an implementation of checkpointing for the reverse or adjoint mode of computational differentiation. ACM Trans. Math. Softw. 26, 1 (March 2000), 19–45. DOI:https://doi.org/10.1145/347837.347846\n\n[2] Philipp Stumm and Andrea Walther. 2010. New algorithms for optimal online checkpointing. SIAM J. Sci. Comput. 32, 2, 836–854. DOI:https://doi.org/10.1137/080742439","title":"References"}]
}
